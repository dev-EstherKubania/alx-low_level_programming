#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @x: first input
 * @y: second input
 * Return: multiplication result of the 2 inputs
 */
int mul(int x, int y)
{
	return (x * y);
}
